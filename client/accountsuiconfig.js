AccountsTemplates.configure({
  defaultTemplate: 'myLoginForm',
//  defaultLayout: 'myLayout', // replace this with your layout if you have one
  // other configuration options...
});


Accounts.config({
  forbidClientAccountCreation: false,
});
